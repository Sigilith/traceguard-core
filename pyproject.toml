[project]
name = "traceguard"
version = "1.0.1"
description = "Zero-trust governance and audit framework"
authors = [{name="Ky"}]
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

dependencies = [
    "fpdf2>=2.7.0",
    "pytest>=7.0.0"
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["traceguard"]
